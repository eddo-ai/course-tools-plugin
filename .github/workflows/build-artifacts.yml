name: Build Artifacts

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'build/**'
      - 'eddolearning-course-tools.zip'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
          run_install: false
      
      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - uses: actions/cache@v3
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader

      - name: Verify vendor directory
        run: |
          if [ ! -d "vendor" ]; then
            echo "Error: vendor directory not found"
            exit 1
          fi
          echo "Vendor directory contents:"
          ls -la vendor/

      - name: Bump version
        run: pnpm version:patch

      - name: Build
        run: pnpm build

      - name: Copy vendor files to build
        run: |
          cp -r vendor build/
          echo "Build directory contents after copying vendor:"
          ls -la build/
          echo "Vendor directory in build:"
          ls -la build/vendor/

      - name: Create plugin zip
        run: wp-scripts plugin-zip --include vendor/

      - name: Verify zip contents
        run: |
          unzip -l eddolearning-course-tools.zip
          if ! unzip -l eddolearning-course-tools.zip | grep -q "vendor/"; then
            echo "Error: vendor directory not found in zip"
            exit 1
          fi

      - name: Commit changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add package.json eddolearning-course-tools.zip eddolearning-course-tools.php
          git commit -m "chore: update build artifacts and bump version" || echo "No changes to commit"
          git push

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: eddolearning-course-tools.zip
          tag_name: v${{ env.NEW_VERSION }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}